server:
  port: ${SERVER_PORT:8071}

spring:

  application:
    name: micro_configserver

  profiles:
    active: native
  cloud:
    config:
      server:
        native:
#          search-locations: "classpath:/config"
          search-locations: "file:///home//juniormartins//Workspace//SharedFiles"

